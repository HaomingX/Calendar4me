# 数据库连接（默认使用项目目录下的 SQLite 文件）
# DATABASE_URL=sqlite:///./itinerary.db

# SMTP 邮箱配置
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your_username
SMTP_PASSWORD=your_password
SMTP_USE_TLS=true
SMTP_USE_SSL=false
EMAIL_SENDER=reminder@example.com

# 调度器轮询间隔（秒），默认 60
# REMINDER_POLL_INTERVAL=60
